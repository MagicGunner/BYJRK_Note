<Window
    x:Class="ContextMenuDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ContextMenuDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance local:MainWindowViewModel,
                                     IsDesignTimeCreatable=True}"
    mc:Ignorable="d">
    <Window.Resources>
        <local:BindingProxy x:Key="BindingProxy" Data="{Binding }"></local:BindingProxy>
    </Window.Resources>
    <Grid>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Orientation="Vertical">
            <TextBox Text="Binding with RelativeSource">
                <TextBox.ContextMenu>
                    <ContextMenu ItemsSource="{Binding Items}">
                        <!-- <ContextMenu.ItemTemplate> -->
                        <!--     <DataTemplate> -->
                        <!--  <MenuItem Header="{Binding}"  -->
                        <!--  Command="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, Path= DataContext.FooCommand}"  -->
                        <!--  CommandParameter="{Binding}">  -->
                        <!--         </MenuItem> -->
                        <!--     </DataTemplate> -->
                        <!-- </ContextMenu.ItemTemplate> -->
                        <ContextMenu.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding}" />
                                <Setter Property="Command"
                                        Value="{Binding RelativeSource={RelativeSource AncestorType=ContextMenu}, Path=DataContext.FooCommand}" />
                                <Setter Property="CommandParameter" Value="{Binding}"></Setter>
                            </Style>
                        </ContextMenu.ItemContainerStyle>
                    </ContextMenu>
                </TextBox.ContextMenu>
            </TextBox>
            <TextBox Text="Binding with BindingProxy">
                <TextBox.ContextMenu>
                    <ContextMenu ItemsSource="{Binding Items}">
                        <ContextMenu.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding}" />
                                <Setter Property="Command"
                                        Value="{Binding Source={StaticResource BindingProxy}, Path=Data.FooCommand}" />
                                <Setter Property="CommandParameter" Value="{Binding}"></Setter>
                            </Style>
                        </ContextMenu.ItemContainerStyle>
                    </ContextMenu>
                </TextBox.ContextMenu>
            </TextBox>
        </StackPanel>
    </Grid>
</Window>