<Window
    x:Class="WindowCloseDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:WindowCloseDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    Loaded="MainWindow_OnLoaded"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="32"
            Text="{Binding Title}" />
        <StackPanel
            Margin="5"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Orientation="Horizontal">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Height" Value="32" />
                    <Setter Property="MinWidth" Value="32" />
                </Style>
            </StackPanel.Resources>
            <Button Content="_">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:ChangePropertyAction
                            PropertyName="WindowState"
                            TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            Value="{x:Static WindowState.Minimized}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Button Margin="5,0" Content="[ ]">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:ChangePropertyAction
                            PropertyName="WindowState"
                            TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                            Value="{x:Static WindowState.Maximized}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Button
                Background="LightCoral"
                Click="ButtonBase_OnClick"
                Content="X(View)">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:CallMethodAction MethodName="Close"
                                            TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Button Background="LightCoral" Content="X(EventTrigger)">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <i:CallMethodAction MethodName="SaveData" TargetObject="{Binding}" />
                        <i:CallMethodAction MethodName="Close"
                                            TargetObject="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Button
                Background="LightCoral"
                Command="{Binding CloseCommand}"
                Content="X(委托)" />

            <Button
                Background="LightCoral"
                Command="{Binding CloseWithParameterCommand}"
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Content="X(传窗口)" />
        </StackPanel>
    </Grid>
</Window>