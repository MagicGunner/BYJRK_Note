<Window x:Class="DataGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:DataGrid"
        d:DataContext="{d:DesignInstance local:MainWindowViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <DockPanel>
            <StackPanel Margin="5" Orientation="Horizontal" DockPanel.Dock="Bottom">
                <Button Content="增加员工" Command="{Binding AddEmployeeCommand}" />
                <Button Content="删除员工" Command="{Binding DeleteEmployeesCommand}"
                        CommandParameter="{Binding ElementName=DataGrid, Path=SelectedItems}" />
                <Button Content="计算薪水综合" Command="{Binding GetSumSalaryCommand}"
                        Margin="5 0 0 0" />
                <Button Content="选中高薪" Command="{Binding CalculateCommand}"
                        Margin="5 0 0 0" />
                <hc:TextBox x:Name="FilterTextBox" Width="200" Margin="5 0 0 0"
                            Text="{Binding Key, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            hc:InfoElement.Placeholder="Filter data by name"
                            InputMethod.IsInputMethodEnabled="False" />
            </StackPanel>
            <DataGrid Name="DataGrid"
                      d:ItemsSource="{Binding Employees}"
                      ItemsSource="{Binding EmployeeCollection}"
                      SelectionMode="Extended"
                      CanUserDeleteRows="False"
                      AutoGenerateColumns="False">
                <i:Interaction.Behaviors>
                    <local:SelectedItemsBehavior BindableSelectedItems="{Binding SelectedItems, Mode=OneWayToSource}" />
                </i:Interaction.Behaviors>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource DataGridRowStyle}">
                        <Setter Property="IsSelected" Value="{Binding IsSelected}" />
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
                    <DataGridTextColumn Header="FirstName" Binding="{Binding FirstName}" />
                    <DataGridTextColumn Header="LastName" Binding="{Binding LastName}" />
                    <DataGridTextColumn Header="Birthday" Binding="{Binding BirthDay}" />
                    <DataGridTextColumn Header="Salary" Binding="{Binding Salary}" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>