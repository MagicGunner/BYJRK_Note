<Window
    x:Class="ValueConvertersDemo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:ValueConverters;assembly=ValueConverters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ValueConvertersDemo"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="600"
    mc:Ignorable="d">
    <Window.Resources>
        <conv:BoolToVisibilityConverter
            x:Key="BoolToVisibilityConverter"
            FalseValue="Hidden"
            IsInverted="True" />

        <conv:ValueConverterGroup x:Key="UserNameConverterGroup">
            <conv:StringIsNullOrEmptyConverter />
            <conv:BoolToVisibilityConverter FalseValue="Hidden" />
        </conv:ValueConverterGroup>

        <conv:ValueConverterGroup x:Key="AgeConverterGroup">
            <conv:StringToDecimalConverter />
            <conv:IsInRangeConverter MaxValue="99" MinValue="18" />
            <conv:BoolInverter />
            <conv:BoolToVisibilityConverter FalseValue="Hidden" />
        </conv:ValueConverterGroup>

        <conv:ValueConverterGroup x:Key="PasswordConverterGroup">
            <conv:IsInRangeConverter MaxValue="999" MinValue="8" />
            <conv:BoolInverter />
            <conv:BoolToVisibilityConverter FalseValue="Hidden" />
        </conv:ValueConverterGroup>
    </Window.Resources>
    <Grid>
        <Border
            Padding="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="White"
            BorderBrush="Gray"
            BorderThickness="1"
            CornerRadius="5"
            TextElement.FontSize="18">
            <Border.Effect>
                <DropShadowEffect />
            </Border.Effect>
            <Border.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="Width" Value="150" />
                </Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Width" Value="75" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
                <Style TargetType="CheckBox">
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                </Style>
            </Border.Resources>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>用户名：</TextBlock>
                    <TextBox Name="Username" />
                </StackPanel>
                <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                    <TextBlock>年龄：</TextBlock>
                    <TextBox Name="UserAge" Text="14" />
                </StackPanel>
                <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                    <TextBlock>密码：</TextBlock>
                    <TextBox Name="Password" />
                </StackPanel>
                <CheckBox
                    Name="Agree"
                    Margin="0,5,0,0"
                    Content="我已阅读并同意用户协议" />

                <StackPanel>
                    <StackPanel.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,5,0,0" />
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="Foreground" Value="Red" />
                        </Style>
                    </StackPanel.Resources>
                    <TextBlock
                        Visibility="{Binding ElementName=Username, Path=Text, Converter={StaticResource UserNameConverterGroup}}">
                        用户名不能为空！
                    </TextBlock>
                    <TextBlock
                        Visibility="{Binding ElementName=UserAge, Path=Text, Converter={StaticResource AgeConverterGroup}}">
                        年龄需要在18~99之间！
                    </TextBlock>
                    <TextBlock
                        Visibility="{Binding ElementName=Password, Path=Text.Length, Converter={StaticResource PasswordConverterGroup}}">
                        密码长度不能小于8位！
                    </TextBlock>
                    <TextBlock
                        Visibility="{Binding ElementName=Agree, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}">
                        您需要勾选同意用户协议！
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>