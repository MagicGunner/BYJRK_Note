<Window x:Class="DPAndAP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DPAndAP"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <!-- <Style TargetType="local:CustomTextBox"> -->
        <!--     <Style.Triggers> -->
        <!--         <Trigger Property="IsHighlight" Value="True"> -->
        <!--             <Setter Property="Background" Value="Yellow"></Setter> -->
        <!--         </Trigger> -->
        <!--     </Style.Triggers> -->
        <!-- </Style> -->
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <local:CustomTextBox Grid.Row="0" Text="MissBlue" IsHighlight="True" Margin="5" />
        <local:CustomTextBox Text="MagicGunner" Margin="5">
            <Grid.Row>1</Grid.Row>
            <local:CustomTextBox.IsHighlight>True</local:CustomTextBox.IsHighlight>
        </local:CustomTextBox>
        <local:CustomTextBox Grid.Row="2" local:TextBoxHelper.Title="TypeMoon"
                             Text="{Binding RelativeSource={RelativeSource Self}, Path=(local:TextBoxHelper.Title)}"
                             Margin="5" />

    </Grid>
</Window>